# Questwheel v13.4.1

Hotfix only. No new features or theme changes.

- Restores the missing `toggleBlockMode()` helper.
- Fixes the JavaScript error that prevented the + Block dialog from opening.
- Keeps all v13.4 data and behavior compatible.

Upload the v13.4.1 files over the current deployment, then open once with `?v=13.4.1`.
